# Kafka config
kafka.server=localhost:9092
kafka.security.protocol=SASL_PLAINTEXT
kafka.sasl.mechanism=SCRAM-SHA-256
kafka.service.name=SparkStreamKafka
kafka.user=
kafka.password=
kafka.groupId=spark-stream-group
kafka.clientId=spark-stream-producer
kafka.auto.offset.reset=earliest
kafka.topics.in=test
kafka.secure=false

#Redis stream config
spark.redis.mode=Standalone
spark.redis.sentinel.master=
spark.redis.auth=
spark.redis.host=10.15.10.123:6379
redis.topic.stream=
redis.stream.key=
redis.stream.od.input=r
redis.engine.channel=
redis.topic.out=
redis.topic.log=
redis.consumer.group=
redis.consumer.name=
redis.consumer.size=3
redis.channel.data=false

#poll connection
dataSource.uri=
dataSource.user=
dataSource.password=
dataSource.driver=
dataSource.minimum.idle=3
dataSource.pool.size=10
dataSource.cachePrepStmts=true
dataSource.prepStmtCacheSize=1000
dataSource.prepStmtCacheSqlLimit=2048
dataSource.offset.batch.size=50000
dataSource.select.batch.size=5
dataSource.offset.stream.size=5000

#message
size.message.out=20971520

